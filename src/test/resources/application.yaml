spring:
  datasource:
    url: jdbc:mysql://localhost:3306/pay_amigo_app_test
    username: root
    password: root
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
      connection-url: jdbc:mysql://localhost:3306/pay_amigo_app_test
      dialect: org.hibernate.dialect.MySQLDialect